server {
  listen 80;
  server_name _;

  # Serve Vite build output.
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # SPA fallback: client-side routes should resolve to index.html.
    try_files $uri $uri/ /index.html;
  }
}
